# Runtime dependencies — Phase 3 (PostgreSQL).
#
# Deliberately small. The rest of the stack from spec §4 arrives with the phase
# that needs it: celery + redis in Phase 7, the provider SDKs
# (brave/scrapegraph/openai) in Phases 4–6.

fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.5
pydantic-settings==2.7.1

# Storage (spec §23). asyncpg because every request path is async; alembic
# because the schema has to be versioned from the first table onwards.
sqlalchemy[asyncio]==2.0.37
asyncpg==0.30.0
alembic==1.14.1
