# Front-end configuration. Everything here ships to the browser, so it must
# never contain a provider key.

# mock → in-browser fixtures, works with nothing else running (Phase 1)
# api  → the FastAPI backend (Phase 2+): docker compose up -d backend
NEXT_PUBLIC_DATA_SOURCE=api

NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
