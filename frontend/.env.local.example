# Front-end configuration. Everything here ships to the browser, so it must
# never contain a provider key.

# mock → in-browser fixtures (Phase 1)
# api  → FastAPI backend (Phase 2+)
NEXT_PUBLIC_DATA_SOURCE=mock

NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
